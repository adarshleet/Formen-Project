<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css2?family=Abel&family=Alegreya+Sans+SC:wght@700;800&family=Red+Hat+Text:wght@300&family=Source+Sans+3:wght@400;500;600;700&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/css/products.css">
    </head>
    <body>
        <main class="container">
            <div class="container">
                <form class="mx-1 my-4 mx-md-4 form-control p-3" id="signup-form" method="post" action="/admin/couponEdit">
                    <div class="mb-4">
                        <h5 class="fw-bold">Edit Coupon details</h5>
                    </div>
                    <div class="d-flex flex-row align-items-center mb-3">
                        <div class="form-outline flex-fill mb-0">
                            <label class="sign-form-label" for="form3Example1c">Coupon
                                Name</label>
                            <input type="text" name="couponName" value="<%= coupon.couponName %>" id="form3Example1c"
                                class="form-control" />
                        </div>
                    </div>
                    
                    <div class="d-flex flex-row align-items-center mb-3">
                        <div class="form-outline flex-fill mb-0">
                            <label class="sign-form-label" for="form3Example4cd">Minimum
                                Purchase</label>
                            <input type="number" value="<%= coupon.minimumPurchase %>" min="0" name="minPurchase"
                                class="form-control" />
                        </div>
                    </div>
                    <div class="d-flex flex-row align-items-center mb-3">
                        <div class="form-outline flex-fill mb-0">
                            <label class="sign-form-label" for="form3Example4cd">Maximum
                                Discount</label>
                            <input type="number" value="<%= coupon.maximumDiscount %>" min="0" name="maxDiscount"
                                class="form-control" />
                        </div>
                    </div>
                    <div class="d-flex flex-row align-items-center mb-3">
                        <div class="form-outline flex-fill mb-0">
                            <label class="sign-form-label" for="form3Example4cd">Starting
                                Date</label>
                            <input type="date" min="0" value="<%= coupon.lastDate %>" name="date" class="form-control" />
                            <input type="text" name="coupon_id" value="<%= coupon._id %>" hidden>
                        </div>
                    </div>
                    
                    <div class="mb-3 mt-4 mb-lg-4 d-grid gap-2">
                        <button type="submit" id="submitButton"
                            class="btn btn-primary btn-lg">Save</button>
                    </div>
                </form>
            </div>
        </main>
<%- include('./foot.ejs') %>
